"""안전 경계 고지 (Phase K3).

이 도구의 검증(이중코일 제거·Z3 인터락 증명·도달성)은 **논리 보조**이며,
기능 안전(functional safety)을 보장하지 않는다.  실무 조사 결과(docs/research)
와 표준(ISO 13849 / IEC 62061 / IEC 60204-1 / NFPA 79)에 따르면, E-stop·가드·
정역 전원차단 같은 안전 기능은 표준 PLC 래더의 소프트웨어 인터락이 아니라
하드와이어 안전릴레이 / 안전인증 PLC로 전원을 차단해 구현해야 한다.

`SAFETY_NOTICE` 를 서버 응답·문서·UI 에 노출해 오해(검증=안전인증)를 막는다.
"""

from __future__ import annotations

SAFETY_NOTICE: str = (
    "⚠️ 안전 경계: 본 도구의 검증(이중코일 제거·인터락 증명·도달성)은 "
    "논리 보조이며 기능 안전을 보장하지 않습니다. E-stop·가드·정역 전원차단 등 "
    "안전 기능은 표준 PLC 래더의 소프트웨어 인터락이 아니라 하드와이어 "
    "안전릴레이 또는 안전인증 PLC로 전원을 차단해 구현해야 합니다"
    "(ISO 13849 / IEC 62061 / IEC 60204-1 / NFPA 79). "
    "생성된 코드는 SIL2+ 안전로직에 그대로 사용하지 말고 유자격 엔지니어가 "
    "반드시 검토·검증하십시오."
)

# 안전 관련 패턴(비상정지·인터록 등)에 자동 첨부할 짧은 주석.
SAFETY_INLINE_HINT: str = (
    "// 안전주의: 인터록은 논리 보조일 뿐 안전기능이 아님 — "
    "E-stop/정역 전원차단은 하드와이어 안전릴레이로 구현(ISO 13849/IEC 62061)"
)


def safety_payload() -> dict[str, str]:
    """API 응답용 안전 고지 페이로드."""
    return {"notice": SAFETY_NOTICE}
